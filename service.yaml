apiVersion: v1
kind: Service
metadata:
  name: homeappservice
spec:
  type: NodePort
  selector:
    app: homeappdeployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 32000
